# Software Engineering

> "In software design, there is no silver bullet—only scenarios." 

This statement reflects my perspective, inspired by Fred Brooks’ seminal essay, **"No Silver Bullet—Essence and Accidents of Software Engineering."** Brooks, the recipient of the 1999 Turing Award, is widely regarded as a pioneer in software engineering.

He is also the author of The Mythical Man-Month, often referred to as the Bible of software development. Additionally, Brooks is renowned for leading the team that developed IBM’s OS/360 operating system.

## Top Principles
I understand that rules evolve over time and do not remain fixed. We must continuously update ourselves. However, there are certain fundamental principles that remain important, and many people have failed by overlooking these guidelines. Learn them before delving into software development. 
I believe there is no harm in doing so, but please ensure you apply them with wisdom. Thank you.

There is a guy said below and I think it is also improtant
> Principles, patterns, and practices are not final purposes.</br> 
> The good and proper application of each is therefore inspired and constrained by a superior, more final purpose.</br>
> **You need to understand why you’re doing what you’re doing!**</br>
> (The POAP is not exempt from the POAP.)

People often misuse concepts because they don’t fully understand what they are doing. A concrete example is provided below.

Nowadays, people frequently talk about SOLID principles and focus on traits, structs, and composition. However, the creator of the term OOP (Object-Oriented Programming), Alan Kay, stated that naming it OOP was a mistake. He emphasized that it is more about `message passing` between objects rather than today’s focus on type hierarchies and static relationships.

The ultimate target for the software developing and please bear these in minds!
1. `Enhanced Software Quality`
2. `Increased Productivity`
3. `Improved Collaboration`
4. `Reduced Technical Debt`
5. `Enhanced Agility`
6. `Cost Savings`

### 1. SOLID Principles
1. S - Single Responsibility Principle (SRP):

    Each class should have one, and only one, reason to change—meaning it should have a single responsibility.</br>

    **What does this mean?**</br>
    It means that a class should be responsible for just one role. Following this principle is crucial to avoid unintended side effects.
    
    For example, suppose a class handles two roles—buyer and seller. These roles often have the conflict rules. If a new developer, unaware of the side effects, modifies the code to make a change for the buyer, it could unintentionally cause a million-pound loss for the seller.
    
    This scenario highlights why the class does not adhere to SRP and demonstrates the importance of separating responsibilities.

2. O - Open/Closed Principle (OCP):

    Software entities should be open for extension but closed for modification.

    > You should be able to extend the behavior of a system without having to modify that system.</br>
    > -- Uncle Bob(the auhtor of Agile Manifesto)

    This principle simply means that we should be able to add new features or behaviours to our system without altering the existing code.

    **Example:**
    Imagine we have a class that calculates discounts. Initially, it supports percentage-based discounts. Later, we need to add a new type of discount—say, a fixed-amount discount.

    **Bad Approach (Violating OCP):**
    We modify the existing class to add a condition for the new discount type. This change could unintentionally break existing functionality.

    **Better Approach (Following OCP):**
    We create a new class for the fixed-amount discount, extending the behaviour of the system without modifying the original class.

    By following OCP, we reduce the risk of introducing bugs and make the code easier to maintain and extend in the future.

3. L - Liskov Substitution Principle (LSP):

    > Objects of a superclass should be replaceable with objects of a 
    > subclass without affecting the correctness of the program.
    > 
    > -- Barbara Liskov's essay
    
    1. Precondition - Children should have more relaxed input constraints (or at least the same)
       Why? If children have more strict constraint, it won't be able the input that parents have.
    2. Postcondition - Children should have stricter output constraints (or at least the same)
       Why? If children have more relax output, it will violate the parents' expectation
    3. Invariants - Children must preserve the restrictions (invariants) of the parent.
       It is a constant and `must` rule in the parent so does children.

    Be cautious when using inheritance. It is a highly dependent feature, and even a small mistake can cause a subclass to behave in unexpected ways. Once the system is fully developed, fixing such issues can turn into a nightmare.

    Rust does not have inheritance like traditional object-oriented languages (e.g., Java or C++). Instead, it uses traits and composition to achieve **polymorphism**. This design inherently encourages adherence to LSP by avoiding some of the pitfalls caused by inheritance in other languages.

4. I - Interface Segregation Principle (ISP):
    
    Clients should not be forced to depend on interfaces they do not use.

    When traits that you do not need are changed, you may be affected by these changes—even without realising it. Essentially, you can be broken by dependencies you do not use.

    This highlights the importance of avoiding unused dependencies.

    In Rust, developers are encouraged to design smaller, more focused traits as part of the API(code level) specification. This approach ensures that clients depend only on what they need, reducing the risk of being impacted by unrelated changes and making the codebase easier to extend and maintain.
    
5. D - Dependency Inversion Principle (DIP):
    
    High-level modules should not depend on low-level modules; both should depend on abstractions.

    High-level code (business logic) should not directly depend on low-level code (implementation details).
    ```rust
    // Bad Example
    struct FileLogger;

    impl FileLogger {
        fn log(&self, message: &str) {
            println!("Log to file: {}", message);
        }
    }

    struct Application {
        logger: FileLogger, // Direct dependency on FileLogger
    }

    impl Application {
        fn run(&self) {
            self.logger.log("Application started."); // Hardcoded dependency
        }
    }

    // Follow DIP
    // Abstraction (Trait) - Both high-level and low-level modules depend on this.
    trait Logger {
        fn log(&self, message: &str);
    }

    // Low-level module 1
    struct FileLogger;

    impl Logger for FileLogger {
        fn log(&self, message: &str) {
            println!("Log to file: {}", message);
        }
    }

    // Low-level module 2
    struct DatabaseLogger;

    impl Logger for DatabaseLogger {
        fn log(&self, message: &str) {
            println!("Log to database: {}", message);
        }
    }

    // High-level module
    struct Application<T: Logger> {
        logger: T, // Depend on abstraction (trait) instead of concrete type
    }

    impl<T: Logger> Application<T> {
        fn run(&self) {
            self.logger.log("Application started.");
        }
    }
    ```

### 2. DRY - Don't Repeat Yourself
### 3. KISS Principle (Keep it Simple, Stupid)
Write simple and straightforward code. Avoid unnecessary complexity to make the software easier to understand and maintain.
### 4. YAGNI Principle (You Aren’t Gonna Need It):
Don’t add functionality until it is absolutely necessary. `Premature optimization or feature development leads to wasted effort and complexity.`
### 5. Separation of Concerns (SoC)
### 6. Encapsulation
Hide internal implementation details and only expose what is necessary, promoting data protection and reducing dependencies.
### 7. Modularity
Break the software into smaller, self-contained modules that can be independently developed, tested, and maintained.
### 8. Test-Driven Development (TDD):
Write tests before writing the actual code to ensure the code meets the requirements and is bug-free.
### 9. Design for Scalability and Maintainability:
Ensure that the software can handle growth in data, users, and complexity over time without requiring complete redesigns.

<!-- ## The Folder structure design
> There are only two hard things in Computer Science: cache invalidation and naming things.
> -- Phil Karlton

Naming things is one of the hardest parts of software development—and folder structure design makes it even trickier.

Why? Because the way we organize folders defines how we group related code and what modules (mods) we create. In turn, this structure directly affects how many names we need and how well they make sense. A good structure helps us reduce complexity, while a bad one leaves us drowning in poorly named files and fragmented logic. -->

# Design Pattern
The pattern is not a specific piece of code, but a general concept for solving a particular problem.

Why is it important?</br>
Design patterns are a toolkit of tried and tested solutions to common problems in software design. Therefore, we don't walk through the same path that people walked through for us just like using the libraries, DRY. Additionally, design patterns become the common language when it comes to software engineering. People know what you are going to say. Vice versa, you understand what people are going to solve. However, I do not intend to introduce the well-known 23 design patterns. Rather, I'd like to talk about another hot topic for infrastructure design in software development in terms of microservices. 

## Architecture design patterns
Before discussing the different architecture, a backend project has many well-known fundamental designs.
### MVC
In the monolithic software design, there are three things here -- MVC, Singleton,

# Glossary 
1. Polymorphism - a fundamental concept in object-oriented programming (OOP) and systems programming that allows entities like functions, methods, or objects to take multiple forms. It enables code to be more flexible, scalable, and maintainable by allowing `a single interface to support different underlying data types or behaviours`.

# References
1. https://fullscale.io/blog/software-engineering-principles/
2. https://fragiledeveloper.com/2019/06/09/the-principle-of-applying-principles-(POAP).html
3. https://medium.com/%E7%A8%8B%E5%BC%8F%E6%84%9B%E5%A5%BD%E8%80%85/%E4%BD%BF%E4%BA%BA%E7%98%8B%E7%8B%82%E7%9A%84-solid-%E5%8E%9F%E5%89%87-%E5%96%AE%E4%B8%80%E8%81%B7%E8%B2%AC%E5%8E%9F%E5%89%87-single-responsibility-principle-c2c4bd9b4e79
4. https://medium.com/%E7%A8%8B%E5%BC%8F%E6%84%9B%E5%A5%BD%E8%80%85/%E4%BD%BF%E4%BA%BA%E7%98%8B%E7%8B%82%E7%9A%84-solid-%E5%8E%9F%E5%89%87-%E9%96%8B%E6%94%BE%E5%B0%81%E9%96%89%E5%8E%9F%E5%89%87-open-closed-principle-f7eaf921eb9c
5. https://refactoring.guru/design-patterns
6. https://rust-unofficial.github.io/patterns/idioms/dtor-finally.html
7. 
